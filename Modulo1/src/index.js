import React from "react";
import ReactDOM from "react-dom";

const app = React.createElement('div', null, 'Olá');

const rootElement = document.getElementById("root");
ReactDOM.render(app, rootElement);
